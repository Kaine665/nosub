POSTGRES_DB=nosub
POSTGRES_USER=nosub
POSTGRES_PASSWORD=replace_with_a_long_random_password
DATABASE_URL=postgresql://nosub:replace_with_a_long_random_password@db:5432/nosub
PORT=3100
PADDLE_ENVIRONMENT=production
PADDLE_API_KEY=
PADDLE_WEBHOOK_SECRET=
# Comma-separated Paddle Price IDs for the official NoSub Pro plans. Access fails closed if empty.
NOSUB_PRO_PRICE_IDS=pri_monthly,pri_quarterly,pri_annual
PAID_ACCESS_GRACE_HOURS=72
CHECKOUT_LINK_SECRET=replace_with_at_least_32_random_characters
# Comma-separated when production and local extension IDs use separate OAuth clients.
GOOGLE_OAUTH_CLIENT_ID=replace_with_google_chrome_extension_client_id.apps.googleusercontent.com
